
#pragma once

namespace Realisim
{
namespace Reactor{
    class Broker
    {
    public:
        Broker() = default;
        Broker(const Broker&) = delete;
        Broker&operator=(const Broker&) = delete;
        ~Broker() = default;
        
    protected:
        // scene
        // camera
        // stats
    };
}
}